import 'package:first_assignment/Column.view.dart';
import 'package:first_assignment/view/dashboard.dart';
import 'package:first_assignment/view/rich_text_view.dart';
import 'package:flutter/material.dart';

class myAssignment extends StatelessWidget {
  const myAssignment({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: ColumnView(),
    );
  }
}
